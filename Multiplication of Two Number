//Write a programm to Multiplication of two number
#include<stdio.h>
int main()
{
	int a,b,c;
	printf("Enter first number: ");
	scanf("%d",&a);
		printf("Enter first number: ");
	scanf("%d",&b);
	
	c=a*b;
	printf("Add=%d",c);
	return 0;
}
